# 标注「优化二期」59 项实施状态

## 目标
在 `docs/优化二期.md` 中标注 59 项优化项的当前实施状态，明确「哪些已经做了」。背景：`docs/qa_material_optimization_plan.md`（一期 12 项）已 100% 实现并验证通过；二期清单在设计上**严格排除一期 12 项**，故二期项本身均未被一期直接完成，但部分项的「一期依赖/基础」现已就绪。本任务用四级状态标记把这一事实在文档中显式化，便于后续排期。

## 现状分析（基于代码审计，非假设）

### 一期 12 项（已全部完成，作为二期项的依赖基础）
MMR 重排序、多策略重试(relax_scope)、Embedding 忠实度、回答一致性、自适应长度、prompt 元数据、文件哈希去重、图片质量预检、字段标准化、LLM 字段提取兜底、跨字段校验(`validate_cross_fields`)、智能缺失材料提醒。

### 二期 59 项审计结论
- **完全已完成（✅）：0 项**。二期清单按设计排除一期 12 项，逐项核对代码后无任何二期项被完整实现。
- **部分实现（🟡）：1 项** — D5。一期 11 已实现 `validate_cross_fields` 并接入 `_send_review_summary`（REVIEW 阶段阻止 + progress 展示），但 D5 要求的「视觉识别/上传后即时调用」未接入。
- **一期基础已就绪（🔵）：18 项** — 与一期有交叉引用、一期相关项已完成，二期项本身尚未开始但可立即动手。
- **待实施（⬜）：40 项** — 未开始，无一期依赖（或仅松散协同）。

### 关键代码审计证据（已读源码确认，非推断）
| 项 | 结论 | 证据 |
|---|---|---|
| E4 bug | ⬜ 未修 | `openai_client.py:97-99` history 注入为 `- {h}`（无角色前缀），`context_rewrite.py:39-43` 依赖 `startswith("助手:")`，前缀不匹配 bug 仍在 |
| F4 | ⬜ 未做 | `external_webhook.py:118-120` except 分支返回 200，异常被吞，企微不重试 |
| D5 | 🟡 部分 | `validate_cross_fields` 已存在且在 `_send_review_summary` 接入；`material_handler.py`/`handle_file_received` 上传后未即时调用 |
| E6 | 🔵 基础就绪 | `qa_agent.py` 仅有重试轮 `relax_scope`，无首轮关键词自适应 scope |
| F7 | 🔵 基础就绪 | `file_store.py:66-85` 无文件名消毒/magic bytes/resolve 校验；一期 7 哈希去重已存在 |
| F11 | ⬜ 未做 | `qdrant_store.py:41-52` `ensure_collection` 不校验现有 vector_size |
| B3 | ⬜ 未做 | `hybrid_retriever.py:74-84` 一次失败永久禁用，无熔断/恢复 |
| F15 | ⬜ 未做 | `config/settings.py` 无 `field_validator` |
| F17 | ⬜ 未做 | `main.py`/`admin_server.py` 无 `/health` `/ready` 端点 |
| F5 | ⬜ 未做 | `admin_auth.py` 仅登录校验，无 per-roomid 鉴权 |
| E7 | ⬜ 未做 | `openai_client.py:37-46` `chat` 直接 `create`，仅 `timeout=20s`，无重试退避（注：探查代理曾误报 tenacity，经核实 L378-387 实为验证码 OCR 图片 prompt，已纠正） |
| C4 | 🔵 基础就绪 | `openai_client.py` `regenerate_answer` 无 `missing_points` 参数 |

## 标记方案

### 状态图例（4 级）
- ✅ **已完成**：当前代码库已完整实现。
- 🟡 **部分实现**：一期交付了该项的部分能力，但未完整覆盖该项要求。
- 🔵 **一期基础已就绪**：与一期有交叉引用且一期相关项已完成；二期项本身尚未开始，可立即动手。
- ⬜ **待实施**：未开始，无一期依赖（或仅松散协同）。

### 59 项状态表（执行时直接套用）
| 项 | 状态 | 说明 |
|---|---|---|
| A1 | ⬜ | 无一期依赖 |
| A2 | 🔵 | 一期4(一致性)已完成；mask_for_llm 未做 |
| A3 | ⬜ | redactor 未做 |
| A4 | ⬜ | 时效标记未做 |
| B1 | 🔵 | 一期1(MMR)已完成；检索 TTL 缓存未做 |
| B2 | ⬜ | embedder 缓存未改 LRU |
| B3 | ⬜ | Qdrant 无熔断/恢复 |
| B4 | 🔵 | 一期1/3已完成；token/成本 trace 未做 |
| B5 | ⬜ | 热重载未做 |
| C1 | 🔵 | 一期3(忠实度)已完成；answerability 维度未做 |
| C2 | 🔵 | 一期3/4(Embedder)已完成；FAQ 语义命中未做 |
| C3 | ⬜ | token 预算/角色标记未做 |
| C4 | 🔵 | 一期2(多策略重试)已完成；regenerate missing_points 闭环未做 |
| C5 | ⬜ | 探索项 |
| D1 | 🔵 | 一期8(图片预检)已完成；视觉分类扩展未做 |
| D2 | ⬜ | 字段历史表未做 |
| D3 | 🔵 | 一期12(主动提醒)已完成；跨通道聚合未做 |
| D4 | ⬜ | 材料防抖未做 |
| D5 | 🟡 | 一期11 `validate_cross_fields` 已实现并在 REVIEW 接入；上传后即时校验未接入 |
| D6 | ⬜ | 探索项 |
| E1 | ⬜ | 意图准确率监控未做 |
| E2 | ⬜ | 意图 LLM 缓存未做 |
| E3 | ⬜ | 正则外置未做 |
| E4 | ⬜ | bug 未修（见证据） |
| E5 | ⬜ | QA/材料解耦未做 |
| E6 | 🔵 | 一期2 relax_scope(重试轮)已完成；首轮 scope 自适应未做 |
| E7 | ⬜ | LLM 无重试退避（见证据） |
| E8 | ⬜ | 无并发速率控制 |
| E9 | ⬜ | 无 /metrics 告警 |
| E10 | ⬜ | trace 回放工具未做 |
| E11 | ⬜ | 文件命名规范未做 |
| E12 | ⬜ | 存储 TTL 未做 |
| E13 | ⬜ | 材料锁定未做 |
| E14 | ⬜ | 视觉结果缓存未做 |
| E15 | ⬜ | 文件访问鉴权未做 |
| E16 | ⬜ | 后台 PII 脱敏展示未做 |
| E17 | ⬜ | source 优先级未做 |
| F1 | 🔵 | 一期12(消息计数)已完成；乱序/晚到处理未做 |
| F2 | ⬜ | KF 会话生命周期未做 |
| F3 | 🔵 | 一期2(查询层重试)已完成；消息级死信未做 |
| F4 | ⬜ | 回调异常吞掉未修（见证据） |
| F5 | ⬜ | per-roomid 鉴权未做 |
| F6 | ⬜ | H5 CSRF/防重未做 |
| F7 | 🔵 | 一期7(哈希去重)已完成；路径穿越/恶意文件未做 |
| F8 | ⬜ | 错误脱敏/安全头未做 |
| F9 | 🔵 | 一期1(MMR)已完成；FTS5 分词升级未做 |
| F10 | 🔵 | 一期6(prompt 元数据)已完成；评分加权未做 |
| F11 | ⬜ | 向量维度校验未做 |
| F12 | ⬜ | 元数据 hash 未做 |
| F13 | 🔵 | 一期5(长度控制)已完成；tiktoken 估算未做 |
| F14 | 🔵 | 一期6(chunk_kind)已完成；语义边界切分未做 |
| F15 | ⬜ | 配置启动校验未做 |
| F16 | ⬜ | 密钥管理未做 |
| F17 | ⬜ | 健康端点/自检未做 |
| F18 | ⬜ | 优雅关闭未做 |
| F19 | 🔵 | 一期12(内存态计数)已完成；多实例一致性未做 |
| F20 | ⬜ | 依赖固定/扫描未做 |
| F21 | ⬜ | 日志轮转未做 |
| F22 | ⬜ | ICRIS 幂等未做 |

**统计：✅ 0 / 🟡 1 / 🔵 18 / ⬜ 40**

## 具体改动（仅改 `docs/优化二期.md`）

1. **概述段（第 3-10 行附近）追加状态说明**：新增一小段，声明一期 12 项已完成，并给出四级图例 + 统计计数（✅0 / 🟡1 / 🔵18 / ⬜40）。

2. **总览表（第 20-22 行）新增「状态」列**：在「优先级」列后追加「状态」列，填入上表对应标记（✅/🟡/🔵/⬜）。

3. **每个 H3 项标题追加状态标记**：如 `### A1 Prompt 注入防护与 system 加固 ⬜`、`### D5 上传后即时跨字段校验 🟡`。59 个标题逐一追加。

4. **🟡/🔵 项在正文「依赖」行补充一期就绪说明**：对 D5 及 18 个 🔵 项，在各自「依赖」或「现状」末尾追加一行 `> 一期基础：一期X 已完成（见 docs/qa_material_optimization_plan.md）；本项二期部分待实施。`，明确区分「一期已做」与「二期待做」。

5. **不改动**任何代码文件、配置、requirements；仅文档标注。

## 假设与决策
- **判断口径**：以「当前代码库真实状态」为准（已逐项读源码核对 12 个代表项），非一期交叉引用项默认 ⬜，执行时再抽查确认；一期交叉引用项标 🔵（一期部分已做、二期部分待做），D5 标 🟡。
- **不虚标已完成**：经审计 0 项完整完成，如实标注，不因用户预期而虚标 ✅。
- **E7 纠正**：探查代理曾误报存在 tenacity 重试，经人工读 `openai_client.py:37-46` 与 `:372-387` 核实为误读（后者是验证码 OCR 图片 prompt），E7 实为 ⬜。
- **范围限定**：仅标注状态，不展开实施方案、不改代码（与二期清单「分析型」定位一致）。

## 验证步骤
1. 改完后通读 `docs/优化二期.md`，确认 59 个标题均有标记、总览表状态列完整、统计计数一致（0/1/18/40）。
2. 抽查 5 项（D5、E4、F4、E7、F11）确认标记与代码证据一致。
3. 确认未触碰任何 `.py`/`settings.py`/`requirements.txt`。
